/.idea
/node_modules
/build/
/l10n/l10n.pl
/l10n/.transifexrc
/l10n/templates/files_pdfviewer.pot
*.po
.php_cs.cache
.php-cs-fixer.cache

# Composer. Anchored to the project root: an unanchored `vendor/` also silences
# js/vendor/, where the vendored PDF.js build lives, so new files there were
# ignored unless added with `git add -f`.
/vendor/
vendor-bin/**/vendor
vendor-bin/**/composer.lock

# Mac OS
.DS_Store

# Tests - auto-generated files
/tests/acceptance/output
/tests/output
.phpunit.result.cache

# SonarCloud scanner
.scannerwork
